<html> Every page starts and ends with the html opening and closing tag
<head> We can put metadata inside this tag
<body> In this tag everything that the user sees is going into
<title> This tag gives a title to my page
<h1> it's for a header you can only put one h1 element per page
<p> This is a paragraph element
<style> this is a tag for css
<meta> This tag is for metadate
<link> This tag defines the relationship between the current document and an external resource.
<hr> This is a horizontal Rule element
<br> Break line produces a line break
******************IN LINE ELEMENTS ******************
<em> It's for mark the important parts from a text
<strong> It's for mark the special text parts from a text
&nbsp -> It helps to represent a blank space
&copy -> Copyright Symbol
&lt; -> an arrow like this <
&gt; -> an arrow like this >
<abbr> It represents an abbreviation or acronym
<address> Defines the contact information for the author/owner
<ol> This a tag for ordered List
<ul> This is a tag for unordered List
<li> This represents an item in a list
<dl> represents a description list
    <dt> description term
    <dd> description detail
<a> This in an Archor tag, This is used as a link to other pages
<section> This is for define a section
<nav> This tag helps with the navigation
<img> This tag is for images
<figure> This tag specifies self-contained content, like illustrations, diagrams, photos, code listings, etc.
<figcaption> This tag represents a subtitle or caption, can go up or down
<code> This tag help you to display code
<header> This tag Represents a container for introductory content or a set of navigational links
<main> This tag Represents the dominant content of the <body> of a document
<footer>  This tag Represents (author information, copyright information, carriers, etc).
<article> This tag represents a self-contained composition in a document, page, application, or site,
 which is intended to be independently distributable or reusable
 Article vs Section -> Article has a clear topic and section it's more generic
 <aside> This tag defines some content aside from the content it is placed in
 <details> This tag specifies additional details
 <summary> This tag defines a visible heading for the <details> element.
 <mark> This tag defines text that should be marked or highlighted
 <time> This tag defines a specific time (or datetime)
 <div> This tag defines a division or a section in an HTML document without any semantic property
 <span> This tag is a generic inline container element without any semantic property
 <table> This tag defines a html table
 <tr> This tag represents a Table Row Element
 <td> This tag represent a table Data
 <th> This tag  is used for table header cells
 <thead> This tag means table head,  its for a semantic property
 <tbody> This tag means a table body, its for a semantic property
 <tfoot> This tag means a table footer, its for a semantic property
 scope The scope attribute specifies whether a header cell is a header for a column, row, or group of columns or rows
 <form> This tag is used to create an HTML form for user input
 <label>  This tag is used to create a label before an input
 <input>This tag is used to create interactive controls for web-based forms in order to accept data from the user
 <select> This tag  element is used to create a drop-down list
 <optgroup> This tag is used to group related options in a <select> element (drop-down list)
 <option> Represents an item within a select tag
 <detalist> This contains a set of <option> elements that represent the permissible
  or recommended options available to choose
  <fieldset> This tag is used to group related elements in a form
  <legend> This tag creates a tittle for field groups